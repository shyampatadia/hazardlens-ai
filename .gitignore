# Python-generated files
__pycache__/
.pytest_cache/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets
.env

# Remotion pitch video
pitch/node_modules/
# Ignore render output but keep the final narrated cut. The glob matches the
# directory CONTENTS -- a negation cannot escape an ignored directory itself.
pitch/out/*
!pitch/out/hazardlens-pitch-narrated.mp4
